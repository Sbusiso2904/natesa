// export const roles = {
//   admin: "NEC",
//   moderator: "BEC",
//   user: "GENERAL MEMBER",
// };

// export const canEdit = (role) => role === roles.admin || role === roles.moderator;
// export const canViewAll = (role) => role === roles.admin;
// export const canMoveToAlumni = (role) => role === roles.admin || role === roles.moderator;
